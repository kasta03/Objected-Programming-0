// #include "GOperations.h"

// float Suma(const ciag c, rozmiarCiagu rozmiar) {
//     float suma = 0.0;
//     for (int i = 0; i < rozmiar; i++) {
//         suma += c[i];
//     }
//     return suma;
// }

// float Srednia(const ciag c, rozmiarCiagu rozmiar) {
//     return Suma(c, rozmiar) / static_cast<float>(rozmiar);
// }

// float Wykonaj(float (*operation)(const ciag, rozmiarCiagu), const ciag c, rozmiarCiagu rozmiar) {
//     return operation(c, rozmiar);
// }

// void UsunCiagi(ciag c) {
//     delete[] c;
// }
